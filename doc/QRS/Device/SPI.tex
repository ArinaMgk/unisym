
\B{Header}: \verb`c/driver/SPI.h`

There are two kinds of IIC interfaces we can make.

The one is \B{HARD}, if there is an SPI peripheral, just use it.

If there is not, use \B{SOFT} to do the convention.
Note that the delay time depends on your SysTick.
